# ServEcosys Frontend Dependencies (.ssle)

供 `.ssle` 前端应用使用的依赖库。

```
frontend/deps/
├── log/        应用日志
├── ipc/        进程间通信
├── display/    显示操作
├── input/      输入事件
└── app/        应用沙箱
```

构建：`make -C frontend/deps all`
