#
# ServEcosys 移动设备适配模块集
#

config SERVECOSYS_MOBILE_MODULES
	tristate "ServEcosys 移动设备适配模块集"
	depends on PCI || (ARM && ARM64)
	default m
	help
	  ServEcosys 移动设备适配：SoC/触控/传感器/电源/蜂窝网络等支持。
	  作为可插拔模块集，按需加载。
